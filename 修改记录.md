# 修改记录

## 2026-06-23

### 1. AI 模块支持 DeepSeek 官方 API

- **`index.html`**：AI 设置面板新增「API 提供商」下拉框（硅基流动 / DeepSeek 官方），Key 标签和模型名动态切换
- **`js/ai.js`**：
  - 新增 `PROVIDER_CONFIG` 配置两套 API 地址与模型（DeepSeek: `api.deepseek.com` + `deepseek-v4-pro`）
  - 两个提供商的 API Key 独立存储，互不覆盖
  - `callAPI()` 根据当前提供商动态选择 URL 和模型
  - 默认提供商改为 DeepSeek 官方

### 2. 初始化阶段新增快捷场次配置

- **`index.html`**：玩家人数 stepper 下方新增 3 个预设按钮（8人场 / 10人场 / 13人场）
- **`css/styles.css`**：新增 `.preset-row`、`.btn-preset` 样式，游戏开始后自动锁定
- **`js/phase1.js`**：
  - 新增 `PRESET_CONFIGS` 与 `_bindPresets()`，点击预设同步更新人数、阵营输入框、合计校验
  - 通过 `State.updateConfig` 驱动，与原有加减按钮和手动输入完全兼容

| 按钮 | 总人数 | 鹅 | 鸭 | 中立 |
|------|--------|----|----|------|
| 8人场 | 8 | 6 | 2 | 0 |
| 10人场 | 10 | 6 | 2 | 2 |
| 13人场 | 13 | 8 | 3 | 2 |

### 3. 新增 CLAUDE.md

- 记录项目架构、模块加载顺序、三阶段流转、状态管理、部署方式等关键信息

### 4. 地图右键拖动平移 + 目击记录管理优化

- **`js/phase2.js`**：
  - 新增 `_initMapPan()`：右键按住拖动平移地图，屏蔽默认右键菜单，`grabbing` 光标反馈
  - 重写 `_renderSightingList()`：每条记录显示序号徽章、悬停出现修改/删除按钮
  - 新增 HTML5 Drag & Drop：拖拽重排目击记录顺序，联动 `State.reorderPath()` 更新数据
  - 删除调用 `State.removeFromPath()`，修改复用现有浮层 `_openPopover()`
- **`js/state.js`**：新增 `reorderPath(fromIndex, toIndex)` 方法
- **`css/styles.css`**：
  - `.map-wrapper` 添加 `cursor: grab` / `.panning` 时 `grabbing`
  - 新增 `.sighting-item-row`、`.sighting-seq`、`.sighting-action-btn` 等样式
  - 拖拽反馈：`.sighting-dragging`（半透明）、`.sighting-drag-over`（蓝色虚线边框）
  - 操作按钮默认隐藏，hover 时显示；修改蓝调、删除红调
